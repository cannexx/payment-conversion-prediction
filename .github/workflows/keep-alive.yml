# 보조 슬립 방지 수단. 2026-09-07부터 cron-job.org(외부 무료 크론 서비스)를
# 10분 간격 메인 핑으로 병행 운영 중 — GitHub scheduled workflow는 저장소 활동이
# 적으면 실행이 지연/스킵될 수 있어 이 워크플로우만으로는 신뢰하기 어려움.
name: Keep Streamlit App Alive

on:
  schedule:
    # 매 10분마다 실행 (UTC 기준)
    - cron: '*/10 * * * *'
  workflow_dispatch:  # Actions 탭에서 수동 실행도 가능

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit app
        run: |
          curl -s -o /dev/null -w "%{http_code}" https://payment-conversion-prediction-kfmzlsbdapywsfresbxfkm.streamlit.app/
